# Локально (без Docker): URL к PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/leads?schema=public"

# Воркер (опционально)
# WORKER_POLL_MS=2000
# WORKER_LOCK_TTL_MS=120000
# WORKER_MAX_ATTEMPTS=5
