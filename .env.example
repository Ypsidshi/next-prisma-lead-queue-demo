# Локально (без Docker): URL к PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5435/leads?schema=public"

# Локальный dev (если не 3000): PORT=3001

# Воркер (опционально)
# WORKER_POLL_MS=2000
# WORKER_LOCK_TTL_MS=120000
# WORKER_MAX_ATTEMPTS=5
